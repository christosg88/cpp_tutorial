#include <vector>

/// Adds vec1 and vec2 and returns the result
std::vector<int> sum_vecs(std::vector<int> const &vec1, std::vector<int> const &vec2);

/// Prints the vector to the standard output
void print_vec(std::vector<int> const &vec);
